Recommending to run the app with the nodejs which is above 14.

React app could run after "npm install", "npm start". more common script commands are in the package.json. 

Express app could run in dev with "npm run dev". a running mysql server is presuming. the data about the mysql server, like password or database name, could be modified in the express app -- constants.ts file.